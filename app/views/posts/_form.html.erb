<%= form_for @post do |f| %>
	<%= f.label :post  %>
	<%= f.text_field :content %>
	<!-- whatever is sent into this post field, this is the field in the database it
	should update --> 

	<%= f.submit %>

<% end %>